import React from "react";
import { View } from "react-native";

interface ITestReviewProps {}
const TestReview: React.FC<ITestReviewProps> = () => {
  return <View>TestReview</View>;
};

export default TestReview;
